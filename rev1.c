#include <stdio.h>

int main(){
    // operações

    int x,y;
    printf("Digite dois numeros inteiros: ");
    scanf("%d %d", &x, &y);
    printf("Soma: %d\n", x + y);
    printf("Subtração: %d\n", x - y);
    printf("Multiplicação: %d\n", x * y);
    if (y != 0);
    return 0;
    








}